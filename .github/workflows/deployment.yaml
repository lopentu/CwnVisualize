on:
  push:
    branches:
      - main
      - develop
  workflow_run:
    workflows: [Convert Original CWN Base Data]
    types:
      - completed
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: ./cwn-web-2
    steps:
      - name: Checkout repository
        uses: actions/checkout@v2

      - name: Set up Node.js
        uses: actions/setup-node@v2
        with:
          node-version: 16.x
          cache: yarn
          cache-dependency-path: ./cwn-web-2/yarn.lock
      - run: pwd
      - name: Install and Build 🔧 # This example project is built using npm and outputs the result to the 'build' folder. Replace with the commands required to build your project, or remove this step entirely if your site is pre-built.
        run: yarn install && yarn build
#       - run: git config --global user.email "sam880222@gmail.com"
#       - run: git config --global user.name "sam880222"

#       - run: yarn deploy
#       - name: Deploy to gh-pages
#         uses: crazy-max/ghaction-github-pages@v2
#         with:
#           target_branch: gh-pages
#           build_dir: build
#         env:
#           GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      - name: Deploy
        uses: JamesIves/github-pages-deploy-action@v4.2.3
        with:
          branch: gh-pages # The branch the action should deploy to.
          folder: ~/cwn-web-2/build # The folder the action should deploy. 
